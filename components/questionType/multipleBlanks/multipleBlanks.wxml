<view class="mulBlanks">
    <view class="mulBlanks_row" wx:for="{{title}}" wx:key="{{item.id}}">
        <view class="title">{{item.value}}</view>
        <input type="text" placeholder="请输入"/>
    </view>
</view>